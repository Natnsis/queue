import "package:flutter/material.dart";

class HomeSingleTab extends StatelessWidget {
  const HomeSingleTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Home"));
  }
}
