import "package:flutter/material.dart";

class DetailQueue extends StatelessWidget {
  const DetailQueue({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Queues"));
  }
}
